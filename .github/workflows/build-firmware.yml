name: Build a firmware

on:
  workflow_call:
    inputs:
      keyboard:
        type: string
        required: true
      keymap:
        type: string
        required: true

jobs:
  build:
    name: Build a firmware ${{ inputs.keyboard }}:${{ inputs.keymap }}

    runs-on: ubuntu-22.04
    container:
      image: ghcr.io/qmk/qmk_cli:latest

    steps:
    - name: Checkout source
      uses: actions/checkout@v4
      with:
        ref: feature/emacs
        fetch-depth: 1
        submodules: recursive
    - name: Fetch all tags
      run: git fetch --tags
    - name: Checkout feature/emacs branch
      run: git checkout feature/emacs
    - name: Add safe directory
      run: git config --global --add safe.directory /__w/qmk_keychron/qmk_keychron
    - name: Install QMK
      run: qmk setup
    - name: Install Python dependencies
      run: /usr/bin/python3 -m pip install -r requirements.txt
    - name: PWD
      run: pwd
    - name: list files
      run: ls -la ./keyboards/keychron/${{ inputs.keyboard }}/keymaps/${{ inputs.keymap }}
    - name: Compile and link
      run: qmk compile -kb keychron/${{ inputs.keyboard }} -km ${{ inputs.keymap }}
    - name: Archive built firmware
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.keyboard }}-${{ inputs.keymap }}-firmware
        path: __qmk__/*.hex
